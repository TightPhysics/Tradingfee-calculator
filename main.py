import Presentation.Ui.gui as gui
from Presentation.Ui.Frames.NavigationFrame.navigation_frame import NavigationFrame

if __name__ == "__main__":
    app = gui.App()
   # navigation_frame = NavigationFrame(app_instance=app)
    app.mainloop()
